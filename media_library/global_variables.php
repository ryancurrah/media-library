<?

$database = "mediaserver_db";  //name of the database
$con = mysql_connect("localhost","root","password"); // Mysql database connecion information
//$doNotAdd[] = "$entryName == 'ntldr' || $entryName == 'NTDETECT.COM' || $entryName == '.' || $entryName == '..'";

?>